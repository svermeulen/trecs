# 14 — Aspect Interfaces

Aspect interfaces (composing `IRead<T>` / `IWrite<T>` constraints) so one system can iterate different templates that share a capability.

Documentation: https://svermeulen.github.io/trecs/samples/14-aspect-interfaces/
