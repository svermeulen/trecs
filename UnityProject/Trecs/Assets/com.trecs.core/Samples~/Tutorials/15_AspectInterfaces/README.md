# 15 — Aspect Interfaces

Aspect interfaces (composing `IRead<T>` / `IWrite<T>` constraints) so one system can iterate different entity types that share a capability.

Documentation: https://svermeulen.github.io/trecs/samples/15-aspect-interfaces/
